# Cloudflare Pages configuration for Binary Markets Frontend
# Deploy: cd frontend && bun run build && wrangler pages deploy dist

name = "binary-markets"
compatibility_date = "2024-01-01"

[site]
bucket = "./dist"

# Environment variables for production build
# Set these in Cloudflare Dashboard > Pages > Settings > Environment variables:
# - VITE_RPC_URL = https://rpc.moderato.tempo.xyz
# - VITE_SERVER_URL = https://binary-markets-api.fly.dev
